# AGOS test credentials — copy to .env.test and fill in values (never commit .env.test)
#
# Reference values (hardcoded in config.sh, shown here for documentation):
#   API_BASE  = https://dashboard.agos-app.com
#   BRAND_ID  = plasmaide
#   SUPABASE_URL = https://wgfrtkezensrxcjoplih.supabase.co

# Supabase service role key — Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=

# CRON_SECRET — must match CRON_SECRET in Vercel environment variables
CRON_SECRET=
